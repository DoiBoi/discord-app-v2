# HOW TO USE THIS DISCORD APP
Run these commands in a terminal after pulling/cloning 
```
npm install
node deploy-commands.js
node index.js
```
When debugging, use `node index.js --debug` instead. *Doesn't do much but it will show a debug presence*